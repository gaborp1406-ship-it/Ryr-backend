import { SupabaseClient } from '@supabase/supabase-js';
import { DataSource } from 'typeorm';
export declare class AriService {
    private dataSource;
    private readonly supabase;
    private readonly logger;
    private readonly host;
    private readonly port;
    private readonly username;
    private readonly password;
    private readonly app;
    private readonly recordingsBasePath;
    constructor(dataSource: DataSource, supabase: SupabaseClient);
    private getEnv;
    private get url();
    private get auth();
    private isNotFound;
    getInfo(): Promise<any>;
    crearRegistroLlamada(idTrabajador: number, id_etapa_lead: number, tipo_historial: number): Promise<any>;
    obtenerNumeroSalida(idTrabajador: number): Promise<string>;
    call(agent: string, phone: string, idTrabajador: number, id_etapa_lead: number, tipo_historial: number, channelId: string): Promise<any>;
    originate(endpoint: string, args: string, callerId?: string): Promise<any>;
    createBridge(): Promise<any>;
    addChannelToBridge(bridgeId: string, channelId: string): Promise<any>;
    answer(channelId: string): Promise<void>;
    hangup(channelId: string): Promise<any>;
    deleteBridge(bridgeId: string): Promise<any>;
    startBridgeRecording(bridgeId: string, recordingName: string): Promise<any>;
    stopBridgeRecording(recordingName: string): Promise<any>;
    existeGrabacionStored(recordingName: string): Promise<boolean>;
    descargarGrabacionARI(recordingName: string): Promise<Buffer>;
    eliminarGrabacionStoredARI(recordingName: string): Promise<void>;
    buildRecordingPath(recordingName: string): string;
    subirGrabacionSupabase(grabacionNombre: string, fileBuffer: Buffer): Promise<string>;
    iniciarGrabacionDb(idRegistroLlamada: number, grabacionNombre: string): Promise<void>;
    finalizarGrabacionDb(idRegistroLlamada: number, grabacionPath: string, estado?: 'completada' | 'error'): Promise<void>;
}
